import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "publication-dark",
  name: "Publication Dark",
  category: "editorial",
  description: "Publication Dark template",
  features: ["tags", "newsletter-cta"],
  typography: "serif",
  colorScheme: "dark",
};
