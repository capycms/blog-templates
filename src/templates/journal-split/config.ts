import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "journal-split",
  name: "Journal Split",
  category: "editorial",
  description: "Journal Split template",
  features: [],
  typography: "serif",
  colorScheme: "light",
};
