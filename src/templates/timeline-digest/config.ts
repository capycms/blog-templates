import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "timeline-digest",
  name: "Timeline Digest",
  category: "editorial",
  description: "Timeline Digest template",
  features: [],
  typography: "sans-serif",
  colorScheme: "light",
};
