import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "one-column-light",
  name: "One Column Light",
  category: "minimal",
  description: "One Column Light template",
  features: [],
  typography: "sans-serif",
  colorScheme: "light",
};
