import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "webinar-sync",
  name: "Webinar Sync",
  category: "business",
  description: "Webinar Sync template",
  features: ["newsletter-cta"],
  typography: "sans-serif",
  colorScheme: "light",
};
