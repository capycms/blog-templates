import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "grid-dots",
  name: "Grid Dots",
  category: "minimal",
  description: "Grid Dots template",
  features: [],
  typography: "sans-serif",
  colorScheme: "light",
};
