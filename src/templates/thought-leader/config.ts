import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "thought-leader",
  name: "Thought Leader",
  category: "business",
  description: "Thought Leader template",
  features: ["author-bio", "newsletter-cta", "related-posts"],
  typography: "sans-serif",
  colorScheme: "light",
};
