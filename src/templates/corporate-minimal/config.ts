import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "corporate-minimal",
  name: "Corporate Minimal",
  category: "business",
  description: "Corporate Minimal template",
  features: ["author-bio"],
  typography: "sans-serif",
  colorScheme: "light",
};
