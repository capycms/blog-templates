import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "text-first",
  name: "Text First",
  category: "minimal",
  description: "Text First template",
  features: ["reading-time", "toc"],
  typography: "sans-serif",
  colorScheme: "light",
};
