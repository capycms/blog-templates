import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "resource-hub",
  name: "Resource Hub",
  category: "business",
  description: "Resource Hub template",
  features: ["tags"],
  typography: "sans-serif",
  colorScheme: "light",
};
