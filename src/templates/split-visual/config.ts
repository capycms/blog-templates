import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "split-visual",
  name: "Split Visual",
  category: "creative",
  description: "Split Visual template",
  features: [],
  typography: "sans-serif",
  colorScheme: "light",
};
