import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "outline-only",
  name: "Outline Only",
  category: "minimal",
  description: "Outline Only template",
  features: ["tags"],
  typography: "sans-serif",
  colorScheme: "light",
};
