import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "devlog-terminal",
  name: "Devlog Terminal",
  category: "developer",
  description: "Devlog Terminal template",
  features: ["reading-time"],
  typography: "monospace",
  colorScheme: "dark",
};
