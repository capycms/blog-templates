import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "whitespace-serif",
  name: "Whitespace Serif",
  category: "minimal",
  description: "Whitespace Serif template",
  features: [],
  typography: "serif",
  colorScheme: "light",
};
