import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "stark-monochrome",
  name: "Stark Monochrome",
  category: "minimal",
  description: "Stark Monochrome template",
  features: ["breadcrumbs"],
  typography: "sans-serif",
  colorScheme: "light",
};
