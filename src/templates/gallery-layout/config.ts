import { TemplateConfig } from "@/lib/types";

export const config: TemplateConfig = {
  id: "gallery-layout",
  name: "Gallery Layout",
  category: "editorial",
  description: "Gallery Layout template",
  features: ["tags"],
  typography: "sans-serif",
  colorScheme: "light",
};
